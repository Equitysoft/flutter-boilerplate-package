name: core_app
description: App utilities including version helper, restart, preload, and cache cleaner.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.2
  package_info_plus: ^8.1.3
  path_provider: ^2.1.5
  flutter_cache_manager: ^3.4.1
